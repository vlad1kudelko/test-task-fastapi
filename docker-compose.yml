services:
  app:
    build: .
    ports:
      - 8000:8000
    restart: always

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: example
    restart: always

  adminer:
    image: adminer
    ports:
      - 8080:8080
    restart: always
